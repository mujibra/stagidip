import StatusDelivery from './StatusDelivery';

const StatusDeliveryConfig = {
    settings: {
        layout: {},
    }, 
    routes: [
        {
            path: 'apps/statusDelivery', 
            element: <StatusDelivery/>
        }
    ],
};

export default StatusDeliveryConfig;